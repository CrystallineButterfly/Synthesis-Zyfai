# Demo script for YieldMind Engine

        Record this demo after credentials are ready, but keep the narration aligned with
        the existing local skeleton.

        1. Show the repo root and README summary.
2. Open agent.json and explain the safety envelope.
3. Open the Solidity policy contract and point at caps, whitelist, and pause.
4. Run python3 scripts/plan_live_demo.py.
5. Run python3 scripts/run_agent.py with LIVE_MODE=false.
6. Explain where live TxIDs will be pasted after credentials are added.
